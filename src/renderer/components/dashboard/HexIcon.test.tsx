import { describe, it, expect } from 'vitest'
import { HexIcon } from './HexIcon'

describe('HexIcon', () => {
  it('should export HexIcon component', () => {
    expect(typeof HexIcon).toBe('function')
  })
})
