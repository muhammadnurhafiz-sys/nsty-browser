import { describe, it, expect } from 'vitest'
import { UpdateNotification } from './UpdateNotification'

describe('UpdateNotification', () => {
  it('exports UpdateNotification component', () => {
    expect(typeof UpdateNotification).toBe('function')
  })
})
