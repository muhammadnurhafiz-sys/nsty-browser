import { describe, it, expect } from 'vitest'
import { SpaceDots } from './SpaceDots'

describe('SpaceDots', () => {
  it('should export SpaceDots component', () => {
    expect(typeof SpaceDots).toBe('function')
  })
})
