import { describe, it, expect } from 'vitest'
import { NavControls } from './NavControls'

describe('NavControls', () => {
  it('should export NavControls component', () => {
    expect(typeof NavControls).toBe('function')
  })
})
