import { describe, it, expect } from 'vitest'

describe('session store', () => {
  it('should export session functions', async () => {
    // Session store uses Electron APIs, tested via integration
    expect(true).toBe(true)
  })
})
